# Neow Companions Workshop Package

This folder is the Steam Workshop upload workspace for Neow Companions.

Use `ModUploader.exe upload --workspace "<this folder>"` to create or update the Workshop item.

The uploaded content is in `content/` and should include:

- `NeowCompanions.dll`
- `NeowCompanions.json`
- `assets/*.png`

